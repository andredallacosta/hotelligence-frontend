import React from "react";
import { Paper } from "@material-ui/core";

function TableContainer(containerProps) {
  return <Paper {...containerProps} elevation={0} />;
}

export default TableContainer;
